import React from "react";
import "./NotificationPage.scss";

export const NotificationPage = (props) => {
    return <div className = "container">
        <h2>you are registered successfully! Verify your email to log in to system!</h2>
         </div>
};